.onLoad = function(libname, pkgname) {
  message(
    str_c(
      "PhyloWeights contains data from glottolog.com under glottolog's CC-BY 4.0 licence.\n",
      "Available versions are glottolog 4.3 and 4.4. The newest version is used by default."
      ))
}
